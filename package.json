{
  "name": "heroku-ci-cra-test",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "6.10.2",
    "yarn": "0.23.2"
  },
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "rm -rf .cache && CI=true react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
